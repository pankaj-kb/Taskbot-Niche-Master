const randomQuotes = require('./quotes.json');

const generate = () => {
     const index = Math.floor(Math.random() * randomQuotes.length);
     const randomQuote = randomQuotes[index];

     return randomQuote
}

module.exports = {
     generate
};